#!/usr/bin/env bash

# Remove old summaries
rm -r /home/taras/tmp/MoCap/summaries_for_sequences

# Run the program itself
python code/ae/train.py

# Start the tensorboard with the new data
tensorboard --logdir=/home/taras/tmp/MoCap/summaries_for_sequences/ --port 6005


